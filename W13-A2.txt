1. OS
Provides functions for interacting with the operating system, allowing you to manage files, directories, and environment variables.
Example 1: Creating a new directory: os.mkdir("new_folder").
Example 2: Accessing an environment variable: os.getenv("DB_PASSWORD").

2. SYS
Provides access to variables and functions that interact strongly with the Python interpreter, used for manipulating the runtime environment.
Example 1: Accessing command-line arguments: sys.argv.
Example 2: Exiting a script with a specific status code: sys.exit(1).

3. NUMPY
Pckage for scientific computing in Python, offering support for large, multi-dimensional arrays and matrices.
Example 1: Creating a 3x3 matrix of zeros: np.zeros((3, 3)).
Example 2: Performing element-wise operations on arrays (e.g., adding two arrays together).

4. PATHLIB
Handle file system paths.
Example 1: Joining paths safely: path = Path("usr") / "bin" / "python".
Example 2: Checking if a file exists: Path("config.json").exists().

5. DATETIME
Manipulates dates and times, from simple formatting to complex time zone calculations.
Example 1: Getting the current timestamp: datetime.now().
Example 2: Calculating the difference between two dates (Timedelta).

6. LOGGING
Emittes log messages from Python programs, used for debugging and monitoring production applications.
Example 1: Writing an error message to a file: logging.error("Connection failed").
Example 2: Setting different log levels (DEBUG, INFO, WARNING, ERROR, CRITICAL).

7. DJANGO
Web framework for rapid development and clean, pragmatic design.
Example 1: Building a full-featured e-commerce site with a built-in admin panel.
Example 2: Managing database schemas automatically using Django Models and Migrations.

8. FLASK
A lightweight and easy to scale up  web framework for complex applications.
Example 1: Creating a simple "Hello World" web server in just a few lines of code.
Example 2: Building a small microservice that returns JSON data.

9. FASTAPI
A high-performance web framework for building APIs based on standard Python type hints.
Example 1: Building an API with automatic interactive documentation (Swagger UI).
Example 2: Handling asynchronous requests to improve performance under high load.

10. BCRYPT
A library used to securely hash and salt passwords.
Example 1: Hashing a user's password before saving it to a database.
Example 2: Comparing a plain-text password against a stored hash during login.

11. PYTEST
A framework to write small, readable tests, and scales to support complex functional testing.
Example 1: Running simple assert statements to verify function outputs.
Example 2: Using "fixtures" to set up a database state before running a suite of tests.

12. PYLINT
A static code analysis tool which looks for programming errors.
Example 1: Checking if your code follows PEP 8 style guidelines.
Example 2: Detecting unused imports or variables in a large project.